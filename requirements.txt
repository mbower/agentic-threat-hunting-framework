# ATHF Core Dependencies
# These are the minimum requirements to run ATHF CLI

# CLI framework - for building command-line interface
click>=8.0.0

# YAML parsing - for hunt frontmatter and config files
pyyaml>=6.0

# Terminal formatting - for beautiful CLI output
rich>=10.0.0

# Template rendering - for generating hunt files from templates
jinja2>=3.0.0
